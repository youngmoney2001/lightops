export const environment = {
  production: false,
  apiUrl: 'https://app.lightops.cm/api',
  apiVersion: 'v1',
  appName: 'Lightops (Staging)',
  appVersion: '1.0.0',
  debug: true,
  defaultLanguage: 'fr',
  supportedLanguages: ['fr', 'en'],
  tokenRefreshInterval: 300000,
  sessionTimeout: 3600000,
  features: {
    geofencing: true,
    realTimeMonitoring: true,
    notifications: true,
    multiLanguage: true,
    darkMode: true
  }
};
// https://app.lightops.cm/api/v1
